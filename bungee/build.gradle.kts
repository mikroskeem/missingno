val waterfallApiVersion = "1.13-SNAPSHOT"

repositories {
    maven("https://papermc.io/repo/repository/maven-public/")
}

dependencies {
    api("io.github.waterfallmc:waterfall-api:$waterfallApiVersion")
}